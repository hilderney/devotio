{
  "name": "@devocional/backend",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "convex dev",
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit --project tsconfig.schema.json",
    "test": "vitest run",
    "convex:dev": "convex dev"
  },
  "dependencies": {
    "convex": "^1.14.0",
    "better-auth": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.57.0"
  }
}